const detail = () => {

    // let cdata = {};
    // cdata = Object.map((item) => 
    //     <div className="grid" {selData}></div>
    // );
    return (
        <article>

            <nav>
                <ul>
                    <li>dd</li>
                </ul>
            </nav>

        </article>
    );
}
export default detail;